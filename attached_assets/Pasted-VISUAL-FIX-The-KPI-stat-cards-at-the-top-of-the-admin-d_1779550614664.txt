VISUAL FIX: The KPI stat cards at the top of the admin dashboard have large 
circular indicators (next to the numbers like "26 Overdue Tasks", "1 Due Today", 
etc.) that render as 3D glossy spheres with radial gradients and highlights.
Change ALL of them to flat, solid-color circles with no gradients, no shadows, 
no shine effect, and no 3D styling.

FIND the component rendering these circles. They are likely styled with 
one of these patterns — find and replace all of them:

--- PATTERN A: Radial gradient (most likely cause) ---
REMOVE any style like:
  background: radial-gradient(circle at 35% 35%, #ff6b6b, #cc0000)
  background: radial-gradient(circle at 30% 30%, rgba(255,255,255,0.4), transparent)
  background: radial-gradient(ellipse, ...)

REPLACE with solid flat color:
  background-color: #EF4444   (red — for Overdue)
  background-color: #EAB308   (yellow — for Due Today)
  background-color: #F97316   (orange — for Awaiting Client)
  background-color: #3B82F6   (blue — for Pending Approval)
  background-color: #1F2937   (dark/black — for No Category)

--- PATTERN B: Box shadow giving 3D depth ---
REMOVE any:
  box-shadow: inset -3px -3px 6px rgba(0,0,0,0.2), inset 2px 2px 4px rgba(255,255,255,0.3)
  box-shadow: 4px 4px 8px rgba(0,0,0,0.3)
  filter: drop-shadow(...)

REPLACE with:
  box-shadow: none
  filter: none

--- PATTERN C: Pseudo-element highlight/shine ---
REMOVE any ::before or ::after pseudo-elements on the circle 
that add a white highlight spot (the "gloss" reflection effect).

--- FINAL TARGET STYLES for each dot ---
The finished circles should look like this (Tailwind example):

  Red dot:    <span className="w-4 h-4 rounded-full bg-red-500 flex-shrink-0" />
  Yellow dot: <span className="w-4 h-4 rounded-full bg-yellow-400 flex-shrink-0" />
  Orange dot: <span className="w-4 h-4 rounded-full bg-orange-400 flex-shrink-0" />
  Blue dot:   <span className="w-4 h-4 rounded-full bg-blue-500 flex-shrink-0" />
  Black dot:  <span className="w-4 h-4 rounded-full bg-gray-800 flex-shrink-0" />

Or in plain CSS:
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background-color: [solid hex color];
  box-shadow: none;
  flex-shrink: 0;

Also check: the avatar initials circles at the bottom of the page 
(Cameron Drake, Kerry Horncastle, Kelly Chivers) — if those also have 
the same 3D gradient effect, apply the same flat treatment to them:
  Remove radial-gradient, replace with a single solid background color.
  Keep the same color per person, just make it flat.

RESULT: All circular indicators across the dashboard should be 
simple, flat, solid-color circles. No shine, no depth, no gradient.