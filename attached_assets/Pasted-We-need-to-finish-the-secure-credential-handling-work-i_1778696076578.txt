We need to finish the secure credential handling work in the Company Information Hub.

Current state:
- Admin-only onboarding access is now properly split and locked down
- The new admin-managed credential system is encrypted and uses deliberate reveal behavior
- However, the old onboarding `loginCredentials` data is still being stored and passed around as plaintext in the onboarding record/payload

That means the security work is not complete yet.

Goal:
Move client-submitted onboarding login credentials into the secure encrypted credential system and stop treating them as part of the normal onboarding payload.

What needs to change:
1. Migrate existing plaintext onboarding `loginCredentials` into the encrypted `company_credentials` system
2. Stop returning raw `loginCredentials` in the normal onboarding/admin onboarding response
3. Stop editing/storing plaintext login credentials as part of the onboarding edit form/payload
4. Keep credentials accessible to Agency Admins through the secure credentials section only
5. Preserve existing credential data so nothing is silently lost

Important behavior to preserve:
- company owners can still submit login/access details during onboarding if that is part of the onboarding flow
- submitted credentials should be captured and stored securely
- Agency Admins should still be able to view/manage those credentials from the secure credential area
- client-side users should not be able to retrieve stored credentials after onboarding
- the broader onboarding/company info flow should keep working

Implementation guidance:
- treat onboarding-submitted credentials as input that gets written into the encrypted credential system, not as a plaintext field that remains in onboarding storage
- after migration, the onboarding record should no longer expose raw login credentials in normal responses
- if the onboarding record still needs to indicate that credentials were provided, use a safe metadata field such as:
  - credentialsProvided: true
  - credentialsCount
  - credentialsLastUpdated
  but do not include plaintext values
- update the admin CompanyInfoHub so credential management comes from the secure credential system, not onboarding `loginCredentialsJson`
- update any PDF/document/sharepoint generation logic that currently reads onboarding `loginCredentials` so it no longer depends on plaintext onboarding credential data
- if existing plaintext onboarding credentials exist, migrate them safely into encrypted credentials and then clear/remove the plaintext source

Important constraints:
- do not lose existing submitted credentials
- do not expose credential values in broad onboarding/company info responses
- do not break client onboarding submission flow
- do not break the admin Company Information Hub
- keep credentials separate from normal company notes/profile/reference info
- keep the implementation focused on completing the credential security cleanup

Please provide:
1. where plaintext onboarding credentials are still being used
2. how they were migrated into the encrypted credential system
3. how onboarding responses were cleaned up
4. how the CompanyInfoHub now gets credential data
5. how any PDF/sharepoint/document generation was updated
6. a QA checklist covering:
   - existing company with old plaintext onboarding credentials
   - new onboarding submission with credentials
   - admin viewing credentials through the secure credentials area
   - onboarding/company info responses no longer leaking raw credentials
   - client-side users blocked from retrieving credentials